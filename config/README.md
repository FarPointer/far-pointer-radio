# Config

Tool and service configuration files.

## Subdirectories

| Directory | Contents |
|---|---|
| `spinitron/` | Spinitron API configuration templates and playlist export settings |
| `vscode/` | Shared VS Code workspace settings for this repository |

> **Note:** Never commit API keys or secrets. Use environment variables or a secrets manager and store only template/example files here.
