<?php
/**
 * Discarded Convergence Zone Twenty Twenty-Five child-theme prototype.
 *
 * Only exists to ship templates/single-playlist.html — no functions are
 * needed here. Kept as an (almost) empty functions.php so the child theme is
 * self-documenting about why it exists.
 */

if (!defined('ABSPATH')) {
    exit;
}
