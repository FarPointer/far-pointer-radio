# Manual corrections to repeat detection.
#
# Repeats are found by track overlap at a 0.60 threshold. That works because unrelated
# consecutive weeks overlap around 0.00-0.08 while real replays land at 0.60-1.00, but a
# repeat with a substantially revised track list can fall below the line, and two
# themed shows drawing on the same small catalogue can rise above it.
#
# Review the 0.40-0.95 band in reports/repeats.md. Pairs are full broadcast ids.
#
# forced:
#   - ["2024-03-12T22:30:00-0700", "2025-03-25T20:30:00-0700"]
# suppressed:
#   - ["2024-01-30T22:30:00-0800", "2024-12-24T22:30:00-0800"]

# Reviewed 2026-08-05 against the 0.40-0.95 band in reports/repeats.md. The 0.60
# threshold was left as-is: nothing in the data falls between 0.47 and 0.63, so the exact
# cut point is not load-bearing. 2025-04-22 as a rerun of 2025-04-08 only two weeks later
# was queried and confirmed -- its 0.71 understates the match, because several apparent
# differences are spelling variants of one artist (wanubale / wnbl_, [arsonist] /
# {arsonist}).

forced:
  # 2024-06-18 was listed here as a replay of 2024-06-11 until 2026-08-06. It is not,
  # and the correction is worth recording because the evidence is unusual.
  #
  # Episode 058 was recorded for 06.18.2024 as a full two hours. Only its second hour
  # aired that night; the first hour was held back and used on 2024-08-20, where Jim's
  # workbook says so outright: "HOUR ONE: Using the first hour originally recorded for
  # Ep.58 06.18.2024, never aired". The rest of the 06-18 broadcast was a replay of
  # 06-11, which is why the two share 10 of 22 tracks and score 0.46 -- midway between
  # the 0.00-0.08 unrelated-week band and the 0.60+ replay band.
  #
  # The eleven artists named in the Ep.058 promo copy split cleanly along that seam:
  # Sandcastles, Purple Decades, Sverre Knut Johansen and Domenica Diavoleria aired on
  # 06-18, and MIDI Janitor, Bluhm, LEATHERS, Broken Social Scene, Somafree Institute,
  # Noble Oak and Destroyer aired on 08-20.
  #
  # Half a new episode is still an episode, and Jim's call is that treating 06-18 as a
  # distinct broadcast is the version a listener can actually follow. The 0.46 score
  # stays below the 0.60 threshold on its own, so no suppression entry is needed.
  []

suppressed: []
